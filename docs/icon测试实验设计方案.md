# UE4 icon 素材测试实验设计方案

---

## 一、现有实验的问题

### 1.1 第一轮：多变量混淆，无法归因

第一轮测试投放了 7 个 icon 变体，结论为"紫色背景胜出"。但 7 个变体之间**至少 4 个维度同时在变**：

| 维度 | 各变体间的差异 |
|---|---|
| 背景色 | 蓝、紫、深蓝、青色等多种 |
| 鲨鱼姿态与表情 | 张嘴角度、身体朝向各不相同 |
| 光效与粒子 | 有的金币飞溅、有的光晕、有的偏暗 |
| 背景元素 | 气泡、海底、宝石、火焰等完全不同 |

**核心问题**：胜出的变体可能赢在鲨鱼表情更具攻击性、光效更抢眼、或构图信息密度更合适——而不是"紫色"本身。多个变量同时变化时，无法判断哪个变量真正起了作用。"紫色胜出"是一个**不可归因的伪结论**。

### 1.2 第二轮：地基不牢 + 维度再次混淆

第二轮在"紫色胜出"的基础上测试文案，存在三个问题：

**问题 1：前提不可靠。** 第一轮的归因本身站不住脚，在此基础上做优化，结论的置信度要打折扣。

**问题 2：不是纯文案测试。** 将顶部大数字"100000"替换为中文文案（"大话联动""劲爆福利"等），不只是换了文字内容，而是整个视觉重心和信息结构都变了——数字与汉字的视觉权重完全不同。测出来的差异分不清是"文案含义"的功劳还是"视觉排版变化"的功劳。

**问题 3：文案类型与措辞混淆。** 4 条文案同时变了信息类型（IP 联动 vs. 利益点）和具体措辞。如果"劲爆福利"赢了，不知道是"利益驱动型 > IP 联动型"，还是仅仅这四个字的表达更好。

### 1.3 共性缺陷总结

| 缺陷 | 后果 |
|---|---|
| 多变量同测不做正交 | 无法归因，结论不可复用 |
| 逐轮淘汰建立在不可靠前提上 | 误差逐轮累积 |
| 变体间差异过大 | 测不出精细洞察，只知道谁赢了，不知道为什么 |
| 只有胜负，没有"为什么" | 无法指导下一批素材的制作方向 |

---

## 二、新的设计思路

### 2.1 核心原则：单次实验只回答一个问题

每一轮测试必须有且仅有一个明确的**待验证假设**。结论应该是"某个因子的某个水平显著优于其他水平"，而不是"某个成品图比另一个成品图好"。

### 2.2 要素拆解

从现有素材中穷举所有视觉变量，按品类经验过滤后，保留 4 个值得测试的核心因子：

| 因子 | 说明 | 测试水平 |
|---|---|---|
| **A. 主色调** | 背景主导色，决定 thumb-stop | 冷蓝 / 紫罗兰 / 暖金 |
| **B. 角色姿态** | 鲨鱼的呈现方式，决定辨识度和情绪感染 | 正面冲击 / 侧身动态 |
| **C. 特效密度** | 画面热闹程度，影响视觉吸引力和信息噪音 | 无（极简）/ 中等 / 丰富 |
| **D. 文字内容** | icon 上的文案，影响利益传达 | 大数字 / 利益文案 |

**已排除的变量及理由：**
- 角色数量（多鲨/群鱼）：本品类无多角色 icon 的先例，不符合用户认知
- 无文字：此前已排除验证过，不再重复

### 2.3 分组策略：高影响因子优先 + 全因子交叉

将 4 个因子按影响力分为两组，每组做全因子实验：

- **第一组（高影响）**：色调 × 角色姿态 → 3×2 = 6 变体
- **第二组（中影响）**：特效密度 × 文字内容 → 3×2 = 6 变体

全因子交叉的优势：不仅能分离每个因子的主效应，还能发现**交互效应**——例如"紫色 + 正面冲击"的组合是否有 1+1>2 的增益。

---

## 三、具体实验方案

### Round 1：色调 × 角色姿态

**目标**：确定最优主色调和最优角色姿态，并检测两者是否存在交互效应。

**控制变量（锁死不动）**：
- 特效密度：中等（适量金币 + 光晕）
- 文字内容：大数字"100000"，位于顶部
- 背景纹理：纯色渐变，无额外元素
- 鲨鱼模型：使用同一个 3D 模型，仅调整姿态

**变体矩阵（6 个变体）**：

| 变体 | A 色调 | B 角色姿态 |
|---|---|---|
| V1 | 冷蓝（海洋蓝渐变） | 正面冲击（张嘴占满画面） |
| V2 | 冷蓝 | 侧身动态（身体倾斜，有速度感） |
| V3 | 紫罗兰（紫→深紫渐变） | 正面冲击 |
| V4 | 紫罗兰 | 侧身动态 |
| V5 | 暖金（橙→红渐变） | 正面冲击 |
| V6 | 暖金 | 侧身动态 |

**分析方法**：

1. **色调主效应**：比较 mean(V1,V2) vs mean(V3,V4) vs mean(V5,V6)
2. **姿态主效应**：比较 mean(V1,V3,V5) vs mean(V2,V4,V6)
3. **交互效应**：观察某个色调下姿态的优劣是否反转（如冷蓝配正面好，但暖金配侧身好）

**产出结论示例**："紫罗兰色调 CTR 显著高于冷蓝和暖金（+0.4pp, p<0.05），正面冲击姿态优于侧身（+0.2pp, p<0.05），无显著交互效应。"

---

### Round 2：特效密度 × 文字内容

**前置条件**：Round 1 数据回收完毕，胜出的色调 + 姿态已确定。

**目标**：确定最优特效级别和最优文字策略，并检测两者的交互效应。

**控制变量（锁死不动）**：
- 色调：Round 1 胜出的色调
- 角色姿态：Round 1 胜出的姿态
- 文字位置：固定顶部
- 鲨鱼模型 / 背景纹理：与 Round 1 胜出变体一致

**变体矩阵（6 个变体）**：

| 变体 | C 特效密度 | D 文字内容 |
|---|---|---|
| V1 | 无（纯角色 + 纯背景） | 大数字"100000" |
| V2 | 无 | 利益文案（如"万倍爆奖"） |
| V3 | 中等（金币 + 光晕） | 大数字"100000" |
| V4 | 中等 | 利益文案 |
| V5 | 丰富（金币 + 粒子 + 满屏光效） | 大数字"100000" |
| V6 | 丰富 | 利益文案 |

**分析方法**：同 Round 1，分别提取主效应和交互效应。

**重点关注的交互**：丰富特效 + 利益文案是否造成信息过载（画面太满 + 文字太多 → CTR 反而下降）。如果存在这种交互，说明特效和文字不能同时"加满"。

---

### 执行规范

| 项目 | 要求 |
|---|---|
| **素材制作** | 设计师必须使用同一个 PSD/AE 模板，仅替换指定变量，严禁"顺手调一下"其他元素 |
| **投放条件** | 所有变体同时段、同地区、同人群投放，不允许分天或分区域跑不同变体 |
| **样本量** | 每个变体至少 5,000 次展示（建议根据 MDE 和基线 CTR 反算精确值） |
| **跑量周期** | 建议至少 3 天，覆盖工作日和周末的流量差异 |
| **主指标** | 提前锁定唯一主指标（CTR / CVR / IPM 三选一），不跑完再挑 |
| **显著性标准** | p < 0.05，且实际差异 ≥ 预设 MDE 才判定为"显著优于" |

### 时间线估算

| 阶段 | 耗时 | 产出 |
|---|---|---|
| Round 1 素材制作 | 2-3 天 | 6 个变体素材 |
| Round 1 投放测试 | 3-5 天 | 色调 + 姿态结论 |
| Round 2 素材制作 | 1-2 天（基于 Round 1 胜出模板微调） | 6 个变体素材 |
| Round 2 投放测试 | 3-5 天 | 特效 + 文字结论 |
| **合计** | **约 2 周** | **4 个因子的最优水平 + 交互效应洞察** |
