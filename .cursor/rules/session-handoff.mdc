---
description: 结构化会话交接——长会话切换时生成交接摘要，保留关键上下文
alwaysApply: false
---

# 结构化会话交接

> 借鉴 Hermes Agent 的上下文压缩策略（Goal/Progress/Decisions/Files/Next）和 Archon 的 Artifact 通信模式。解决"新开会话后要重新解释一遍"的问题。

## 1. 何时触发

以下场景应生成交接摘要：

| 场景 | 触发方 |
|------|--------|
| Agent 建议新开会话时（context-length-reminder） | Agent 主动生成 |
| 用户说"继续上次的"并提供会话代号 | Agent 请求用户粘贴交接摘要 |
| 复杂任务中途需要切换 Agent 代号 | Agent 主动生成 |

## 2. 交接摘要格式（五要素）

```markdown
## 会话交接: Agent{代号}

### 目标 (Goal)
> 一句话描述本会话的核心任务

### 进展 (Progress)
- [x] 已完成的步骤
- [ ] 未完成的步骤
- 当前停在哪一步，停下来的原因

### 关键决策 (Decisions)
| 决策点 | 选择 | 理由 |
|--------|------|------|
| 例：用 SQLite 还是 PostgreSQL | SQLite | 单机部署，无需额外依赖 |

### 涉及文件 (Files)
| 文件 | 状态 | 说明 |
|------|------|------|
| `path/to/file.py` | 已修改 | 添加了 XXX 功能 |
| `path/to/new.py` | 新建 | 用途是 YYY |

### 下一步 (Next Steps)
1. 具体要做的事项 A
2. 具体要做的事项 B
3. 需要注意的风险或待确认事项
```

## 3. 生成规则

### 3a. 信息优先级（什么值得保留）

| 优先级 | 内容 | 示例 |
|--------|------|------|
| **P0 必须** | 未完成的任务和下一步 | "还差 XXX 功能没做" |
| **P0 必须** | 影响后续的决策 | "用户选了方案 B" |
| **P1 重要** | 已修改的文件清单 | 避免新会话重复搜索 |
| **P1 重要** | 遇到的坑和解决方案 | 避免新会话踩同样的坑 |
| **P2 可选** | 已完成的步骤概要 | 用于了解全貌 |
| **跳过** | 具体代码 diff | 新会话可以自己读文件 |
| **跳过** | 工具调用过程 | 中间过程不重要 |

### 3b. 长度控制

- 目标：**50 行以内**
- 超过时：优先压缩"已完成"部分，保留"未完成"和"决策"
- 如果任务非常复杂：写入 `.cursor/artifacts/{agent-code}-handoff.md` 文件，交接摘要中引用文件路径

### 3c. 交接摘要的输出方式

1. **直接输出**：在提醒用户新开会话时，把摘要直接写在消息中
2. **文件存档**（可选）：如果摘要较长或涉及大量文件，写入 `.cursor/artifacts/` 目录

## 4. 新会话接续

新会话的 Agent 收到交接摘要后：

1. 读取"涉及文件"列表中的所有文件，确认当前状态
2. 确认"关键决策"是否仍然有效
3. 从"下一步"第一项开始执行
4. 沿用或更新会话代号（用户指定）

## 5. 与现有规范的关系

| 规范 | 关系 |
|------|------|
| `context-length-reminder.mdc` | 触发时机由其判断；交接摘要由本规范定义格式 |
| `session-focus.mdc` | 建议开新会话时，附带交接摘要 |
| `agentx.mdc` 会话标识 | 交接摘要中的代号即为 Session ID |
| `WORK_LOG.md` | 交接摘要是轻量版，WORK_LOG 是完整版 |
