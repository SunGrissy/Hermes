---
description: 前端编码规范 - JS/HTML/CSS 项目的代码风格与缓存管理
globs: "**/*.js,**/*.html,**/*.css"
alwaysApply: false
---

# 前端编码规范

## 缓存管理
参见 `version-management.mdc` 规范。使用后端 `/api/version` + `Cache-Control: no-cache` 响应头管理缓存，不再使用 `?v=` 手动版本号。
对于 PmSystemApp 等纯前端项目（无后端），仍可使用 `?v=` 或 VersionTracker 方案。

## 代码风格
- 缩进：2 空格
- 字符串：单引号优先
- 文件末尾保留一个空行
- 函数和类使用 JSDoc 注释说明用途，但不要写废话注释

## 模块组织（PmSystemApp 模式）
```
ui/components/   → UI 组件（视图渲染）
business/        → 业务逻辑（数据处理、状态管理）
core/            → 基础工具（logger、api-client）
```
UI 层只负责渲染，业务逻辑放 business 层，不要混在一起。

## 错误处理
- fetch 请求必须有 try/catch 或 .catch()
- 用户可见的错误要有友好的中文提示，不能直接抛 stack trace
