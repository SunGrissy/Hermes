---
description: 验收流程唯一入口 — 用户说"验收"时按此文件从头到尾执行，不可跳步
alwaysApply: true
---

# 验收流程（Acceptance Checklist）

> **本文件是验收的唯一权威流程。** 其他规则文件中的验收相关段落仅做补充说明，执行顺序以本文件为准。

## 触发条件

用户说 **"验收"** 或 **"验收通过"** 时，立即进入以下流程。

---

## 流程总览

```
0. 分支安全 ─────────────── git-branch-guard.mdc
1. 快检（L0 + L1） ──────── regression-testing.mdc
2. 任务注释清理 ─────────── agentx.mdc（.md/.mdc 豁免）
3. WORK_LOG 更新 ────────── agent-core.mdc
4. pre-commit 门禁 ──────── agent-core.mdc C1-C4
5. git add + git commit ─── git-workflow.mdc
6. 功能分支合并（如适用）── git-workflow.mdc + worktree-workflow.mdc
7. 推送（默认执行） ─────── 本文件第七步
```

---

## 第 0 步：分支安全检查

```bash
git branch --show-current
```

- 比对 SESSION_BRANCH，不一致 → **中止，报告用户**
- 详见 `git-branch-guard.mdc` 第 2 节

## 第 1 步：快检（L0 + L1）

按 `regression-testing.mdc` 执行：

**L0 静态检查：**
1. 对所有本会话修改的文件运行 `ReadLints`，新增错误 = 0
2. 枚举/常量引用无遗漏（`Grep` 验证）
3. 缓存版本号已正确处理

**L1 冒烟测试（涉及 UI/API 变更时）：**
1. 健康接口正常返回
2. 主页可加载，核心入口可达
3. Console 新增错误 = 0

**纯文档变更（仅 `.md` / `.mdc` / `.html` 静态文件）** → L1 冒烟测试跳过，L0 中 lint + 引用检查仍执行。

输出快检报告（表格形式），P0 问题 → 停止验收，修复后重新开始。

## 第 2 步：任务注释清理

- 按本会话代号精确匹配删除 `[Agent{代号} Task]` 注释
- **不得误删其他会话标记**
- **豁免**：本会话仅修改文档文件（`.md` / `.mdc`）时，跳过此步

详见 `agentx.mdc`「任务追踪注释规范」。

## 第 3 步：更新 WORK_LOG.md

- 状态标记为 **"验收通过"**
- 格式：`## [日期] - [任务简述]`
- **豁免**：如果项目根目录无 `WORK_LOG.md`，跳过此步

## 第 4 步：pre-commit 门禁

按 `agent-core.mdc` pre-commit 检查：

| # | 检查 | 未通过时 |
|---|------|---------|
| C1 | 暂存文件列表无意外文件 | 提示用户确认或 unstage |
| C2 | 暂存文件无新增 lint 错误 | 修复后重新 stage |
| C3 | 无硬编码敏感信息 | 停止提交，报告位置 |
| C4 | WORK_LOG.md 已更新（如适用） | 先更新再提交 |

## 第 5 步：git add + git commit

1. `git add` 相关文件（优先指定文件路径，避免 `git add .`）
2. 归属判定：入场快照中已脏的文件 → 不提交（详见 `agentx.mdc`「并行开发提交隔离规范」）
3. `git commit`，message 格式：`<type>(<scope>): <中文描述>`

## 第 6 步：功能分支合并（条件触发）

仅当前在功能分支上时执行：

1. `git merge main`（解决冲突 → 见 `git-workflow.mdc` 合并冲突处理流程）
2. 合并后自测（见 `git-workflow.mdc` 合并后测试流程）
3. 测试通过 → `git checkout main && git merge <branch>`
4. 如使用 worktree → 按 `worktree-workflow.mdc` 合并 + 清理（不可跳过）

## 第 7 步：推送（默认执行）

**策略：验收通过即推送到 tygit，不再询问。** 使用 **`git push origin`**（或等价地仅指向 tygit 的 `git push`），**不**推 `github`，除非用户在说「验收」时**同时明确要求**「推 GitHub」/「同步 GitHub 镜像」。

### 7a. 检测变更范围

```bash
git submodule status
git status
```

判定：是否涉及子模块变更？

### 7b. 子模块推送（如有子模块变更）

**对每个有变更的子模块，按依赖顺序逐个执行：**

```
步骤 A：子模块内
  1. cd 进子模块目录
  2. pre-push 门禁（agent-core.mdc P2：敏感路径检查）
  3. git push origin
  4. 如果 rejected → git pull --rebase → 重试一次
  5. 仍失败 → 停下来报告用户，不强制
  6. 记录 push 成功的 commit hash
```

### 7c. 根仓库推送（必须紧跟 7b）

```
步骤 B：根仓库（不可省略，不可延迟）
  1. cd 回根仓库目录
  2. git status → 确认子模块指针已变更
  3. git add <子模块名>（逐个 add 有变更的子模块）
  4. git commit -m "chore: update submodule pointers"
  5. pre-push 门禁（agent-core.mdc P1-P3）
  6. git push origin
  7. 如果 rejected → git pull --rebase → 重试一次
  8. 仍失败 → 停下来报告用户
```

### 7d. 无子模块变更时

直接在当前仓库执行 pre-push 门禁 → `git push origin`（rejected 同理处理）。

### 7e. 用户明确要求推 GitHub 时（可选）

在 7b→7d 完成后，若用户本次验收口令包含「推 GitHub」：在已推送过的同一批仓库内，再对**相同分支**执行 `git push github`（子模块先于父仓，顺序与 7b 一致）。

### 完成判定

以下条件**全部满足**才算推送完成：

- ✅ 每个有变更的子模块远端有最新 commit
- ✅ 根仓库远端的子模块指针指向对应的最新 commit
- ✅ 根仓库本身的变更已推送

**只完成子模块 push 而未更新根仓库 = 未完成。**

---

## 第 8 步：输出验收报告

```markdown
## 验收完成
- 分支：{branch}
- Commit：{hash}（子模块）/ {hash}（根仓库，如适用）
- 快检结论：PASS / PASS(待修)
- 推送状态：✅ 已推送 / ⚠️ 部分失败（见说明）
```

---

## 异常处理速查

| 异常 | 处理 |
|------|------|
| 分支不一致 | 第 0 步中止，报告用户 |
| L0 检查发现 P0 | 第 1 步停止，修复后重新验收 |
| pre-commit 门禁未通过 | 第 4 步修复后继续 |
| push rejected | 自动 `pull --rebase` 重试一次，仍失败则报告 |
| 子模块 push 成功但根仓库 push 失败 | 报告用户，不回滚子模块（已推送无法撤回） |
| 无 WORK_LOG.md | 跳过第 3 步，不报错 |
| 纯文档变更 | 跳过 L1 冒烟 + 任务注释清理，其余照常 |
