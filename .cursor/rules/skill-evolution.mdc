---
description: Skill 自进化规范——Agent 可在任务中自主提议创建、改进 Skill
alwaysApply: false
---

# Skill 自进化规范

> 借鉴 Hermes Agent 的 Skills 自创建/自修复机制。核心理念：**Skill 是活的知识资产，应随使用迭代，而非写完就不动。**

## 1. 何时提议创建新 Skill

Agent 在完成任务后，如果发现以下信号，**应主动向用户提议**创建新 Skill（不自行创建）：

| 信号 | 示例 |
|------|------|
| **重复模式** | 同一类操作在 2+ 个会话中出现（如"每次都要手动检查 X"） |
| **复杂流程** | 单次任务涉及 5+ 步骤且步骤间有依赖关系 |
| **领域知识** | 任务需要特定领域的判断标准（如"什么算好的 Boss 表演"） |
| **用户纠正** | 用户纠正了 Agent 的做法 2+ 次，说明存在隐性知识 |

**提议话术**：
> 这次任务中我注意到 [模式描述]，如果把 [核心知识/流程] 沉淀为一个 Skill，以后类似任务会更快更准。要不要我起草一个？

## 2. Skill 创建流程

用户同意后，按以下流程创建：

```
Step 1: 草稿 — 按 skill-authoring-guide 模板起草 SKILL.md
Step 2: 自检 — 运行 Skill 质量门禁（skill-quality-gate.mdc）
Step 3: 试跑 — 在当前任务上下文中模拟使用一次，验证有效性
Step 4: 用户审阅 — 输出草稿供用户确认
Step 5: 落地 — 写入 .cursor/skills/{name}/SKILL.md + 更新 README.md
```

## 3. 何时提议改进现有 Skill

Agent 在使用 Skill 过程中，如果发现以下情况，**应在任务完成报告中附带改进建议**：

| 情况 | 处理 |
|------|------|
| **Skill 指导与实际做法不一致** | 提议 patch：说明哪里过时，建议怎么改 |
| **Skill 缺少覆盖当前场景** | 提议扩展：说明缺失的场景和建议补充内容 |
| **Skill 中的步骤可以简化** | 提议精简：说明冗余步骤和简化方案 |
| **用户覆盖了 Skill 的建议** | 记录偏差：在任务报告中标注，累积到 2 次则提议修订 |

**改进建议格式**：
```markdown
### Skill 改进建议
- **Skill**: {skill-name}
- **类型**: patch / extend / simplify
- **原因**: {发现了什么问题}
- **建议**: {具体修改内容}
- **影响范围**: {哪些场景会受益}
```

## 4. Skill 版本追踪

每个 SKILL.md 应在底部维护简要变更记录：

```markdown
## 变更记录
| 日期 | 版本 | 变更 | 来源会话 |
|------|------|------|----------|
| 2026-04-11 | v1.0 | 初始创建 | AgentXxx |
| 2026-04-15 | v1.1 | 补充边界场景 | AgentYyy |
```

## 5. 纪律

- **不自动修改 Skill**：所有创建和修改必须经用户确认
- **不重复造轮**：创建前先搜索现有 Skills 是否已覆盖
- **保持自包含**：新 Skill 应能独立使用，不过度依赖其他 Skill
- **README 同步**：创建新 Skill 后必须更新 `.cursor/skills/README.md`
