---
description: Git Worktree 多任务并行规范 - 创建、命名、合并、清理
alwaysApply: false
globs:
---

# Worktree 多任务并行规范

当用户需要同时在多个会话处理不同任务时，使用 Git Worktree 隔离工作目录。

## 何时使用 Worktree
- 用户明确说"我要同时做两个任务"
- 会话启动检查发现当前分支有其他任务的未提交改动，且用户选择并行处理
- Agent **不主动**创建 worktree，必须用户确认

## 创建 Worktree 流程
1. 确保当前分支工作区干净（无未提交改动），否则向用户提供两个选项：
   - **`git stash`**（推荐临时中断）：改动暂存到栈里，恢复时 `git stash pop`。适合"先放一放，待会回来接着做"
   - **`git commit`**（推荐阶段性成果）：改动作为正式提交保留在分支历史中。适合"这部分已经可用，先存档"
   - Agent 需向用户说明两者区别，由用户决定
2. 创建功能分支（如果还没有）：`git branch feat/<任务名>`
3. 创建 worktree：`git worktree add ../<项目名>--<任务名> feat/<任务名>`
4. 告知用户新工作目录路径，建议在新 Cursor 窗口中打开

## 命名规范
- **分支名**：`feat/<简短任务名>`、`fix/<简短任务名>`
- **worktree 目录名**：`<原项目目录名>--<任务名>`
- 示例：`pm-system--import-fix`、`pm-system--ui-polish`

## 合并与清理流程（验收时）
1. 在 worktree 目录完成最终提交
2. 在功能分支上先合并主分支：`git merge main`
   - 如果有冲突 → 按 `git-workflow.mdc` 中「合并冲突处理流程」执行
   - 合并完成后 → 按 `git-workflow.mdc` 中「合并后测试流程」执行
   - 用户确认测试通过后，Agent 再执行下一步
3. 回到主工作目录，合并功能分支：`git checkout main && git merge feat/<任务名>`（此时应为 fast-forward）
4. 删除 worktree：`git worktree remove ../<项目名>--<任务名>`
5. 删除分支：`git branch -d feat/<任务名>`

## Worktree 清理时机（自动触发）
- **每次验收完成时**：合并 + 清理是验收流程的一部分，不可跳过
- **每次会话启动检查时**：Agent 运行 `git worktree list`，如果发现已不存在目录对应的 worktree 记录，执行 `git worktree prune` 并告知用户
- **worktree 数量 ≥ 3 时**：Agent 列出所有 worktree 并建议用户清理不再活跃的

## 注意事项
- 同一个分支不能同时被两个 worktree checkout
- worktree 目录是全新的工作副本，不包含 `node_modules` / `venv` / `__pycache__` 等构建产物。**Agent 在新 worktree 中首次执行构建或运行命令前**，必须先检查依赖是否已安装，如未安装则提醒用户需要执行安装命令（如 `npm install`、`pip install -r requirements.txt`）
- 子模块（pm-system 等）的 worktree 在子模块目录内操作，不影响根仓库
