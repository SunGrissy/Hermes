---
description: 工作空间全局地图 - 各项目定位、技术栈和关键文件
alwaysApply: true
---

# 工作空间地图

本工作空间是"游戏研发自动办公军团"的总司令部，包含多个独立工具项目。

## 项目清单

| 项目 | 用途 | 技术栈 | 入口 |
|------|------|--------|------|
| **PmSystemApp** | 游戏项目管理系统（核心） | 前端: 原生 JS + HTML/CSS; 后端: FastAPI + SQLAlchemy | `index.html` / `backend/main.py` |
| **PerformEval** | 乘法绩效评价系统 | 前端: 原生 JS + HTML/CSS; 后端: FastAPI + SQLite | `index.html` / `backend/main.py`（端口 8112） |
| **CCI-ScoreSystem** | 素材竞争力评分（CCI） | Streamlit Python 应用 | `app.py` |
| **TaskReminderApp** | 任务提醒 + 钉钉推送 | FastAPI + Uvicorn, HTML | `server.py` / `TaskReminder.html`（端口 8000） |
| **FileCleanerTool** | 文件扫描清理桌面工具 | Python + customtkinter | `gui.py` |
| **TeamScore** | 团队评分数据处理 | Python 脚本集（M12、雷达图） | 各子目录下的 `.py` 脚本 |
| **dingtalk-desktop** | 钉钉桌面端消息通道（基础设施） | Python + Frida + msgpack | `daemon.py`（端口 19200） |
| **novel** | 小说正文与设定（**独立 Git 仓，仅 GitHub**） | Markdown | 仓库根 `novel/`，远端 `SunGrissy/novel`；父仓 `.gitignore` 忽略 |
| **roi-forecast** | ROI 精准测算（Streamlit） | Python + Streamlit | `roi_forecast.py`（端口 8501）；**独立 Git 仓**（tygit `roi-forecast`），与父仓并列维护，父仓 `.gitignore` 忽略 |
| **workspace-docs** | 管理规范文档库 | Markdown | `README.md` |

## 关键约定
- 各项目相互独立，无跨项目 API 调用（PerformEval 预留了 PmSystemApp 成员同步接口，尚未对接）
- dingtalk-desktop 是所有子系统的**对外消息通道**，其他系统通过 HTTP 调用 daemon API 发送钉钉消息
- 根目录的 `WORK_LOG.md` 记录跨项目级别的工作日志
- 各项目内部也可维护自己的 WORK_LOG
- `start_army.bat` 是全局服务启动脚本
- PmSystemApp 和 PerformEval 各自有 `AgentReadMe.md`（详细 Agent 工作手册）
- **Agent Skills 体系**：`.cursor/skills/` 为唯一 skill 来源，详见 [.cursor/skills/README.md](../skills/README.md)，换设备克隆仓库即可恢复
