---
description: Shell 环境和 Git 操作规范（Windows PowerShell）- 适用于所有项目
alwaysApply: true
---

# Shell & Git 规范

## PowerShell 环境注意事项
所有项目运行在 **Windows PowerShell** 环境下：
- **不支持** `&&` 连接命令，使用 `;` 代替
- **不支持** heredoc（`<<'EOF'`），git commit 多行消息用 Write 工具写入临时文件再 `git commit -F` 提交
- **不支持** `&` 后台运行，使用 `Start-Process` 或 `block_until_ms: 0`
- 路径用**双引号**包裹：`cd "d:\MyAgents\项目名"`
- Python 启动器：优先用 `py` 而非 `python`（`python` 在部分环境无输出）
- **中文 commit message**：避免直接用 `-m` 传中文（可能乱码），优先使用 `-F` 文件方式
- **print 语句禁用 emoji**：Windows 终端默认 GBK 编码，emoji 会触发 UnicodeEncodeError 导致进程崩溃

## Python 语法检查命令模板
```powershell
py -c "import ast; ast.parse(open(r'文件名.py', encoding='utf-8').read()); print('OK')"
```

## 口令约定

| 用户说 | Agent 执行 |
|---|---|
| "更新" | 执行下方「全量更新流程」 |
| "推送" / "push" | 执行下方「推送流程」（**仅** `git push origin`，tygit） |
| "推 GitHub" / "push github" | pre-push 门禁通过后，在父仓及有改动的子模块内依次 `git push github <当前分支>`（子模块先于父仓），**不**推 `origin` 除非用户同时要求 |

### 推送流程（用户说"推送"时触发）

**不论是否在验收流程中，任何 push 前都必须通过 pre-push 门禁。**

```
1. 分支安全验证
   git branch --show-current → 比对 SESSION_BRANCH

2. pre-push 门禁（agent-core.mdc P1-P3）
   P1: git submodule status → 有 + 前缀 = 子模块有未推送 commit → 先推子模块
   P2: 敏感路径检查
   P3: 推送顺序检查（子模块先于父仓库）

3. 如果 P1 检测到子模块变更 → 按 acceptance-checklist.mdc 7b→7c 执行：
   a. 逐个 cd 子模块 → git push
   b. 回根仓库 → git add <子模块> → git commit "chore: update submodule pointers" → git push
   c. 完成判定：子模块远端 + 根仓库指针 + 根仓库远端 三者同步

4. 无子模块变更 → 直接 `git push origin`（**不要** `git push github`，除非用户本次明确说了「推 GitHub」）

5. rejected → `git pull --rebase`（对 `origin`）→ 重试一次 → 仍失败报告用户
```

### 全量更新流程（用户说"更新"时触发）

```
0. 记录当前目录 (saved_dir)，cd 到工作空间根目录

1. 根仓库 pull
   git pull origin main
   ├─ 冲突 → 报告冲突文件，询问是否继续子模块更新
   └─ 成功 → 继续

2. 根仓库 GitHub 同步
   若有 github remote:
     git fetch github main
     对比 main 与 github/main:
     ├─ 无差异 → 跳过
     └─ 有差异 → 报告 N 个新提交 + git merge github/main
          ├─ 成功 → 标记 github_merged = true
          └─ 冲突 → 报告，等用户处理

3. 逐个子模块更新
   for each submodule:
     ├─ 脏检查 (git status --short)
     │    └─ 脏 → 跳过并报告 "⚠️ <模块> 有未提交改动，已跳过"
     ├─ 分支检查 (git branch --show-current)
     │    └─ 空 (detached HEAD) → git checkout main (或 master)
     ├─ git pull origin <当前分支>
     │    └─ 冲突 → 报告，跳过此模块
     ├─ 若有 github remote:
     │    git fetch github main
     │    对比差异:
     │    ├─ 无差异 → 跳过
     │    └─ 有差异 → 报告 + git merge github/main
     │         ├─ 成功 → 标记
     │         └─ 冲突 → 报告，跳过
     └─ cd ..

4. 子模块指针检查
   cd 根仓库
   git status → 若子模块指针有变更:
     报告哪些子模块指针变了
     询问是否提交指针更新

5. 同步回推 tygit（仅当合入了 GitHub 内容时）
   询问 "是否 push origin 将合并结果同步回 tygit？"
   ├─ 是 → git push origin main
   └─ 否 → 跳过
   （**不**在此步骤自动 `git push github`；镜像 GitHub 仅当用户明确说「推 GitHub」时执行）

6. 回到原目录 (cd saved_dir)，静默恢复

7. 输出更新报告
   ┌──────────────────────────────────────┐
   │ 更新报告                              │
   │ 根仓库:      origin ✅  github ✅     │
   │ pm-system:   origin ✅  github ✅ (3) │
   │ performeval: origin ✅  github 无变化  │
   │ cci_system:  ⚠️ 脏状态，已跳过        │
   │ task_reminder: origin ✅               │
   │ teamscore:   origin ✅                 │
   │ 已同步回推: 是/否                      │
   └──────────────────────────────────────┘
```

**设计要点：**
- 子模块先检查脏状态和分支状态，避免 detached HEAD 下 pull 报错
- GitHub 差异自动 merge 而非仅报告，真正做到"全部更到最新"
- 任何冲突都报告而非静默失败，冲突模块跳过不阻塞其他模块
- 完成后静默恢复原目录，不改变用户工作上下文
- 仅当合入了 GitHub 内容时才询问回推，避免无意义的交互

## Git 操作要点
- Commit 前必须完成自测（参照 agentx.mdc 自测规范和 regression-testing.mdc）
- Commit message 格式：`<type>(<scope>): <中文描述>`（参照 git-workflow.mdc）
- 不使用 `--force`、`--amend`（除非用户明确要求）
- 子模块（submodule）的 `.git` 实际路径需用 `git rev-parse --git-dir` 获取
