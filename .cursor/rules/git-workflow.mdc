---
description: Git 工作流规范 - 提交时机、message 格式、验收流程、分支安全检查
alwaysApply: true
---

# Git 工作流规范

## 会话启动 — 分支安全检查（铁律）

Agent 在每轮会话**首次执行写操作前**（StrReplace / Write / git add / git commit 等），必须完成以下检查：

1. 运行 `git branch --show-current` 确认当前分支
2. 运行 `git status --short` 检查是否有未提交的改动
3. 检查结果处理：
   - ✅ **干净且分支匹配** → 正常工作
   - ⚠️ **有未提交改动，分支与当前任务匹配** → 向用户确认：
     > "当前分支 `feat/xxx` 有 N 个文件未提交。这些是你之前留下的改动吗？确认后我继续在此基础上工作。"
     - 用户确认 → 继续
     - 用户说不是 → 按"分支不匹配"流程处理
   - 🚫 **有未提交改动，分支与当前任务不匹配** → **禁止直接操作**，向用户报告：
     > "当前分支是 `feat/xxx`，有未提交改动 N 个文件。本次任务和这个分支无关，建议先处理当前改动再开始新任务。"
     - 提供选项：
       - (a) **`git stash`**（推荐临时中断）：改动暂存到栈里，恢复时 `git stash pop`。适合"先放一放，待会回来接着做"
       - (b) **`git commit`**（推荐阶段性成果）：改动作为正式提交保留在分支历史中。适合"这部分已经可用，先存档"
       - (c) **创建 worktree**：用独立工作目录并行处理新任务（参见 `worktree-workflow.mdc`）
     - Agent 需说明以上选项的区别，由用户决定
4. 如果当前分支是 `main`/`master` 且任务是新功能/修复 → 提醒用户是否要先创建功能分支

## 何时提交（铁律）
- 只在用户明确说**"请提交"**或**"验收通过"**时才执行 git commit
- 功能做完、Bug 修完、重构完 → 都**不要自动提交**，等用户指令
- 永远不要未经许可执行 `git push`

## Commit Message 格式
```
<type>(<scope>): <中文描述>
```
type: feat / fix / docs / style / refactor / perf / test / chore / revert

示例：
- `feat(ops): 添加运营矩阵导出功能`
- `fix(login): 修复 token 过期后白屏问题`

## "验收通过"完整流程
1. 更新 WORK_LOG.md（状态标记为"验收通过"）
2. `git add` 相关文件（优先指定文件，避免 `git add .`）
3. `git commit` 提交当前分支
4. 如果在功能分支上：
   - 先在功能分支合并主分支：`git merge main`
   - 如果有冲突 → 见下方「合并冲突处理流程」
   - 合并完成后 → 见下方「合并后测试流程」
   - 测试通过后：`git checkout main && git merge <branch>`（此时应为 fast-forward）
5. 告知用户 commit ID，询问是否需要 push

### 合并冲突处理流程
当 `git merge` 产生冲突时：
1. Agent 列出冲突文件清单，对每个冲突给出：
   - **冲突位置**：文件名 + 行范围
   - **冲突描述**：双方各改了什么（用自然语言概括，不只是贴 diff）
   - **建议方案**：保留哪边 / 如何合并，并说明理由
2. 用户逐项选择处理方式，Agent 执行
3. 全部冲突解决后，`git add` + `git commit` 完成合并提交

### 合并后测试流程
合并完成后，进入测试验证阶段：
1. Agent 根据合并涉及的文件，列出**受影响的功能点清单**
2. Agent 逐项自测：检查代码逻辑一致性、引用完整性、lint 无新增错误、关键路径可运行
3. Agent 输出自测报告（通过 / 存疑 / 失败），对存疑项说明原因
4. 用户根据报告确认是否通过，通过后才继续合并回 main

## 禁止提交
- `.env`、API 密钥、密码等敏感信息
- `node_modules/`、`__pycache__/`、`venv/`
- 大型二进制文件
