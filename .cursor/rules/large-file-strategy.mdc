---
description: 大文件生成策略 — 避免 Write 工具因内容过大导致静默失败和无限重试
alwaysApply: true
---

# 大文件分步写入

当需要生成超过 300 行的文件（如完整 HTML 页面）时：

1. **先建骨架**：用 Write 创建包含完整框架（head/style/script/基础结构）的文件，内容区用 `<!-- PLACEHOLDER: xxx -->` 占位
2. **分块填充**：用 StrReplace 逐个替换占位符，每次替换不超过 100 行
3. **禁止一次性写入超大文件**：Write 工具对超长 contents 参数会静默失败，导致无限循环重试

```
❌ BAD: Write(path, 一个 1000 行的完整 HTML)  →  反复失败、循环
✅ GOOD: Write(path, 骨架 300 行) → StrReplace(占位1) → StrReplace(占位2) → ...
```
