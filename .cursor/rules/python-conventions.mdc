---
description: Python 编码规范 - FastAPI 项目风格与安全要求
globs: "**/*.py"
alwaysApply: false
---

# Python 编码规范

## 通用风格
- 遵循 PEP 8，缩进 4 空格
- 类型注解：函数参数和返回值必须加 type hint
- import 顺序：标准库 → 第三方库 → 项目内部模块，各组之间空一行

## FastAPI 项目规范
- 路由文件放 `backend/app/routers/`，用 `APIRouter` 组织
- 请求/响应模型用 Pydantic BaseModel 定义，不要用裸 dict
- 敏感配置（API key、数据库密码）通过环境变量或 `.env` 读取，禁止硬编码

## 错误处理
```python
# 正确：明确的异常处理
try:
    result = await some_operation()
except SpecificError as e:
    logger.error(f"操作失败: {e}")
    raise HTTPException(status_code=500, detail="操作失败，请稍后重试")

# 错误：裸 except 吞掉异常
try:
    result = await some_operation()
except:
    pass
```

## 依赖管理
- 新增依赖后必须更新 `requirements.txt`（带版本号）
- 示例：`fastapi==0.104.1`，不要写 `fastapi` 不带版本
