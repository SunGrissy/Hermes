---
description: 通用回归测试规则 - 验收/收工口令触发的自动回归流程（适用于所有项目）
globs:
alwaysApply: true
---

# 回归测试规则

## 口令触发（必须遵守）

### 用户说"验收" → 执行快检（L0 + L1）

**L0 静态检查：**
1. Lint 新增错误 = 0
2. 枚举/常量引用无遗漏
3. 缓存版本号已正确处理（参照 version-management.mdc）

**L1 冒烟测试：**
1. 健康接口（如 `/api/health`）正常返回
2. 主页可加载，核心入口可达
3. Console 新增错误 = 0

完成后输出快检报告（表格形式，含 PASS/FAIL 和问题分级）。

### 用户说"收工" → 执行全量（L0 + L1 + L2）

在快检基础上增加 L2 全量验证：
1. 各模块主流程验证（模块清单参照项目的 agentx.mdc）
2. 旧数据兼容性验证
3. 异常路径测试（空数据、非法输入、断网重连等）
4. 输出全量报告

## 问题分级

| 级别 | 定义 | 处理方式 |
|---|---|---|
| **P0**（阻断） | 白屏、核心链路不可用、数据覆盖风险 | 立即停止，修复后重跑全部检查 |
| **P1**（中风险） | 次核心流程失败 | "收工"不允许通过；"验收"标记"通过待修" |
| **P2**（低风险） | 文案/样式瑕疵 | 记录待办，不阻断 |

## 快检报告模板

```markdown
## 回归快检报告

### L0 静态
| 检查项 | 结果 | 说明 |
|---|---|---|
| Lint 检查 | PASS/FAIL | 新增错误数 |
| 引用一致性 | PASS/FAIL | |
| 缓存版本号 | PASS/FAIL | |

### L1 冒烟
| 检查项 | 结果 | 说明 |
|---|---|---|
| 健康接口 | PASS/FAIL | |
| 主页加载 | PASS/FAIL | |
| 核心入口 | PASS/FAIL | 验证了 xxx |
| Console 错误 | PASS/FAIL | |

**结论**：PASS / PASS(待修) / FAIL
**P1+ 问题**：（如有列出）
```

## 全量报告模板（L2 追加部分）

```markdown
### L2 全量
| 模块 | 主流程 | 旧数据兼容 | 异常路径 | 说明 |
|---|---|---|---|---|
| 模块A | PASS/FAIL | PASS/FAIL | PASS/FAIL | |
| 模块B | PASS/FAIL | PASS/FAIL | PASS/FAIL | |
| ... | | | | |

**结论**：PASS / FAIL
**问题汇总**：
- P0: （如有）
- P1: （如有）
- P2: （如有）
```

## 模块覆盖范围

各项目的模块清单和对应文件定义在各自的 `agentx.mdc` 中。回归测试时按该清单逐模块覆盖。
