---
description: Agent 核心行为准则 - 沟通语言、工作节奏、门禁检查、工作日志
alwaysApply: true
---

# Agent 核心行为准则

## 沟通
- 始终使用**中文（简体）**回复
- 不要在代码注释中用英文解释"我做了什么改动"

## 工作节奏
- 动手前先说明改动计划（涉及 3+ 文件时必须输出简要方案等用户确认）
- 不确定的技术决策要提出来讨论，不要自作主张
- 每次只做一件事，完成一件再做下一件

## 门禁检查（铁律）

Agent 在执行以下动作前，**必须**先通过对应的门禁检查。任一项未通过 → **停止操作，报告用户**。

> 检查失败时的修复指南见 `preflight-checks` Skill。

### post-edit — 每次 StrReplace / Write 之后

| # | 检查 | 执行方式 | 未通过时 |
|---|------|---------|---------|
| E1 | 被修改文件无新增 lint 错误 | `ReadLints` 被修改文件 | 立即修复，修复不了则报告 |
| E2 | 引用一致性 | 如果改了常量/枚举/接口签名/CSS 变量/函数名 → `Grep` 搜索所有引用方 | 列出需同步修改的位置，逐个处理 |

### pre-commit — 执行 `git commit` 前

| # | 检查 | 执行方式 | 未通过时 |
|---|------|---------|---------|
| C1 | 暂存文件列表无意外文件 | `git diff --cached --name-only` | 提示用户确认或 unstage |
| C2 | 暂存文件无新增 lint 错误 | `ReadLints` 所有被修改的文件 | 修复后重新 stage |
| C3 | 无硬编码敏感信息 | `git diff --cached` 搜索 `secret\|password\|token\|api_key\|private_key` | 停止提交，报告位置 |
| C4 | WORK_LOG.md 已更新 | 检查 `WORK_LOG.md` 是否在暂存列表中 | 先更新再提交 |

### pre-push — 执行 `git push` 前

| # | 检查 | 执行方式 | 未通过时 |
|---|------|---------|---------|
| P1 | 子模块无未推送 commit | `git submodule status` 检查有无 `+` 前缀 | 先进入子模块 push |
| P2 | 敏感路径未被 track | `git ls-files` 检查 `backend/data/`、`.env` | 加入 .gitignore 并 untrack |
| P3 | 推送顺序正确 | 如有子模块变更，子模块必须先于父仓库推送 | 调整推送顺序 |

### on-accept — 用户说"验收"时

| # | 检查 | 执行方式 | 未通过时 |
|---|------|---------|---------|
| A1 | 上述 C1-C4 全部通过 | 执行 pre-commit 全部检查 | 修复后继续 |
| A2 | WORK_LOG 状态标记为"验收通过" | 读取 WORK_LOG.md 确认 | 更新状态 |
| A3 | 上述 P1-P3 全部通过（如果需要 push） | 执行 pre-push 全部检查 | 修复后继续 |

## 工作日志
- 提交代码前**必须先更新** `WORK_LOG.md`（由 C4 门禁强制执行）
- 日志格式：`## [日期] - [任务简述]`，含状态、内容、备注
