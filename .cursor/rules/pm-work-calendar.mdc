---
description: 工作空间内「工作日」与 PM 系统假日/调休一致；禁止默认等同周一至周五。
alwaysApply: true
---

# 工作日口径（PM 日历）

## 原则

- 文档、脚本、定时任务说明中出现 **「工作日」**、**「上一工作日」**、**「是否上班」** 等含义时，**默认与 PmSystem「假日与调休管理」一致**，数据来自 `pm-system/backend/data/gamedev_pm_data.json` 的 `holidays` / `workdays`（与版本规划 `isWorkday` 逻辑一致）。
- **禁止**在不做说明的情况下，把「工作日」写成或实现成「仅周一到周五、忽略法定假与调休上班日」。

## 实现要点

- **PmSystem** 提供 `GET /api/pm-calendar`（`pm-system/backend/app/routers/data.py`），与主数据 JSON 同源。
- 日志助手 **工作日早报**：优先请求 **PmSystem** 该接口，失败再读本地 `gamedev_pm_data.json`；`dingtalk-desktop/work_report_assistant_run.py` + `pm_work_calendar.py`；计划任务 **每日** 触发，由脚本判定是否推送。
- 内网 IP（如 `192.168.20.160:8112`）若指向 PM 后端，即 **PmSystem**，不是 PerformEval。
- 新增/修改涉及工作日判断的代码时：复用同一套字段与判定，并在无日历数据时明确标注「降级策略」。

## 参考

- Skill：`.cursor/skills/pm-work-calendar/SKILL.md`
- 接口：`pm-system/backend/app/routers/data.py`（`/api/pm-calendar`）
- PM 前端：`pm-system/ui/components/version-planning-view.js`（`isWorkday`）
