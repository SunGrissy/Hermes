---
description: 在合适时机提醒用户考虑提交与推送，与 Agent 保持良好版本管理习惯
alwaysApply: true
---

# 版本管理提醒

## 原则

在**自然断点**或**会话收尾**时，若存在未提交变更或已提交未推送的 commit，Agent 应简短提醒用户考虑提交/推送，而不是静默忽略。

## 何时提醒（满足其一即可考虑）

1. **任务收尾**：用户说「先这样」「收工」「好了」「没问题了」或当前任务明确告一段落时。
2. **切换上下文前**：用户明确要开始另一项任务、另一项目或另一会话前。
3. **用户询问进度或交付物**：在回复交付清单时，顺带一句「当前有 N 处未提交 / 有 M 个 commit 未推送，需要我帮你提交并推送吗？」。

不依赖固定轮数；同一会话内**至多主动提醒一次**，除非用户再次进入明显断点。

## 提醒方式

- 先简要说明现状，再给选择，不替用户做决定：
  - 例：「当前 performeval 有 2 个 commit 未推送、根仓库有 1 处未提交（palace/xxx）。需要我帮你执行提交/推送吗？」
- 若用户说「推送」「提交」「帮我提交」等，再按 `git-workflow.mdc` 与 `acceptance-checklist.mdc` 执行，并做 pre-push 门禁（含子模块顺序）。

## 不提醒的情况

- 用户已明确说「先不提交」「暂不推送」。
- 刚完成一次提交或推送，且没有新的未提交/未推送变更。
- 当前对话以「问问题」为主，没有产生可提交的产出。

## 与现有规范的关系

- **提交时机**：仍以 `git-workflow.mdc` 为准（仅在用户明确说「请提交」或「验收通过」时执行 commit）；本规则只负责**提醒**，不自动提交。
- **推送**：用户说「推送」时按 `shell-git.mdc` 推送流程执行；本规则在断点处提醒「是否有未推送 commit，是否需要推送」。
