---
description: 长上下文优先会话内分流（子Agent），必要时再提醒新开会话
alwaysApply: true
---

# 上下文长度提醒

## 原则

在**自然断点**或**上下文负担已经影响回答质量/速度**时，优先尝试在当前会话内分流（子Agent/并行拆解）；仅当分流不合适或收益不明显时，再提醒用户新开会话。目标是让后续回复更稳、更快，而不是机械提示。

用量侧长会话会导致 **Cache Read 占比极高**（反复整段喂入历史上下文）。本规则默认立场：**先分流、后切会话**，仍保持偏保守，但避免“一触发就固定话术”。

## 分流优先判定（新增）

在触发任何「应提醒」信号后，先做一次快速判断：

1. **能否在当前会话内分流**：后续工作是否可由 1 个或多个子Agent承担（如大范围摸底、跨模块检索、可并行实现/核对）？
2. **分流是否更省上下文**：若分流后主线程只需收敛结果并决策，优先直接分流，不先提示新开会话。
3. **不适合分流再提醒切会话**：仅当任务高度依赖连续上下文、需要用户频繁实时拍板、或分流后仍会持续堆积上下文时，才建议新开会话。

执行口径：
- 若判定“可分流” → 直接执行分流并简短告知，不追加“上下文偏长”提醒。
- 若判定“不可分流” → 再按下文触发条件给出切会话提醒。

## 何时必须 / 应当提醒（分层）

### 强触发（满足任一条 → 本条回复收尾处应给出提醒）

除非落入下方「不提醒的情况」，否则**不要省略**简短提醒：

1. **任务边界**：上一事项已交付、验收、或用户明确说「好 / 先这样 / 收工」后，**又提出新的独立需求**（与刚结束项无直接继承关系）。
2. **主题切换**：对话主题从 A 切到 B（例如从代码实现切到另一模块、从排错切到写文档、从项目甲切到项目乙），且 B **无法**用一两句承接语从 A 自然导出。
3. **工具密集 / 多文件探索结束（先分流判断）**：满足小节《工具密集 / 多文件探索 — 强触发口径》时，先执行《分流优先判定》；可分流则直接分流，不强制提醒。仅在不可分流时，在本条助手回复收尾处给出简短新开会话提醒（除非落入「不提醒的情况」）。
4. **长线程上的「第二阶段」**：同一会话里已经完成一个完整子目标（例如方案已定、目录已摸清），将进入**另一套上下文**（大规模改码、长跑测试、多轮交互验收）——在进入前提醒切会话，比进入后再堆历史更省上下文。

### 工具密集 / 多文件探索 — 强触发口径（不设死轮数）

助手自检「本轮是否如此」，满足**任一条**即归入类型 **3**，触发后按「频率」节去重：

- **多文件**：为回答**当前这一条用户消息**，已累计对 **≥3 个不同文件路径**（或等价：同一路径多段 `offset` 读但跨多个逻辑模块仍算多文件意图）做了读取或宽检索，且主要目的是**定位 / 对照 / 汇总**，而非单点一两行修改。
- **工具密集**：在同一用户消息对应的推理链路中，已出现**多轮**工具调用（目录扫、`grep`/`glob`、批量 `read_file`、语义/代码库搜索等），且在上下文中留下较长「现场勘查」式中间结果。
- **收尾可带走**：你已能写出**简短结论**（根因/候选点/下一步路径）；若结论之后还要继续**大范围动刀**或开启新一圈探索，**必须先提醒**再给出操作建议（提醒放在本条末段，避免用户未看到）。

### 弱触发（满足 ≥2 条 → 应提醒；只满足 1 条 → 可提醒）

1. 明显感到需依赖**很多轮前文**或**大量已打开/引用文件**才能答好当前一句。
2. 已出现**边际收益下降**（继续本会话对准确/完整/速度的性价比变低）。
3. 用户单次输入里包含**多个可拆开的独立请求**（建议新会话拆成「一次一事」）。

### 综合判断（与上面分层的关系）

- **优先执行强触发**：不必等「体感很长」。
- **类型 3 与类型 4 同时成立**时，**一条**收尾提醒即可，不必重复两套话术。
- **弱触发**用于强触发未命中、但叠加信号已明显的情形。
- 仍**不依赖固定轮数**；但若强触发已命中，**不要**以「还不算太长」为由跳过提醒。

## 提醒话术（简短、可微调，避免机械复读）

> 当前上下文已经偏长，建议**新开一个会话**继续，把结论或下一步用一两句贴过去，后续会更快更稳。

可替换为同义自然表达，避免每次完全同一句：
- 「这条线程信息量已经很大，切到新会话继续会更稳更快。」
- 「后面会进入新阶段，建议开新会话接着做，响应会更轻。」

**必须**在提醒中标明**当前会话的 Agent 代号**（若本会话已绑定，如 AgentFlow），便于用户在新会话中沿用、验收时统一清理。示例：

> 当前上下文已经偏长，建议**新开一个会话**继续。（**本会话代号：AgentXxx**，新会话可说「本会话沿用代号 AgentXxx」继续。）

若刚好在断点：询问用户是否先提交。
提醒话术后，补充开启下一轮会话可以对 Agent 说什么（含沿用代号的句式），以便用户可以复制粘贴。

## 不提醒的情况

- 用户已说过「就在这会话继续」或「先不新开」
- 本轮已进入子Agent分流且能继续承接主任务（无需再打断建议新开会话）
- 只是简短收尾（例如一次提交、一句确认），没有新一轮复杂讨论
- 本回复已经因**会话聚焦**规则在建议新开会话，无需再重复提「上下文长」
- 当前用户消息**明显是**对上一句的即时澄清（一两句、同一子任务内）——不借机打断
- 虽触达「≥3 文件」或多次工具，但用户意图**明确限定**为单点小改（常量/单行/单测断言等），且本轮**无**摸底式扩展阅读 —— **不按类型 3 强制提醒**（避免打断微操作）。

## 频率

- **强触发**：每种强触发类型在同一会话内**最多提醒一次**（避免同一句复读）；若出现**不同类型**的强触发（例如先「工具密集 / 多文件探索结束」、后又「新独立需求」），**可以各提醒一次**。
- **弱触发**的泛化提醒：同一会话内**至多一次**（与历史版本一致）。
- 用户已拒绝切会话后，**不要**为同类理由反复纠缠；除非出现**新的强触发类型**。
