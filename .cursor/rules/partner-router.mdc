---
description: 数字思考伙伴自动路由——五域分流，域内自治。纯代码/技术任务不路由。
alwaysApply: true
---

# 思考伙伴路由（极简版）

## 原则

用户提出非代码问题时，Agent 自动判断是否需要调用思考伙伴。用户显式指定时无条件遵从。

## 第一级：五域分流

| 域 | 信号词 | 入口 Skill | 域内可按需加载 |
|----|--------|-----------|---------------|
| 决策/方案/风险 | 要不要做、选A还是B、有什么风险、盲区 | `producer-mind` | 芒格(陷阱)、塔勒布(尾部风险)、张小龙(克制)、贝索斯(长期)、Naval(杠杆)、稻盛(人心) |
| 游戏设计/审核 | 策划方案、Boss表演、Feature设计 | `design-review` 或 `design-council` | `cognitive-design` |
| 数据/归因 | 指标异常、为什么XX变了 | `data-investigation-playbook` | `gelman-perspective` |
| 概念/写作/表达 | 解剖概念、想透、降秩、帮我写、白话说 | `thinking-tools` 或 `writing-engine` 或 `plain-speak` | — |
| 其余 | — | 不路由，直接回答 | — |

## 第二级：域内自治

入口 Skill 内部声明「什么时候建议加载哪个协作伙伴」。模型只需在第一级做粗分类（5选1），精细调度由 Skill 正文指引。

## 不路由的情况

- 纯代码/技术实现（写代码、修Bug、配置部署）
- 简单事实查询
- 用户说"不用思考伙伴"或"直接回答"

## 用户覆盖

- "用芒格的视角" → 只用芒格
- "所有伙伴都来说说" → 全员出场
- 显式指定时不自动加其他

## 轻量模式

问题简单时一句话点出核心判断，不走完整 Protocol。

## 红队（可选）

多伙伴讨论后全员共识、零分歧时，可建议启动 `jobs-perspective` 压力测试。用户说"不用"就跳过。
